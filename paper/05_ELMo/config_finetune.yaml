ELMO:
    PATH: '/home/long8v/torch_study/paper/05_ELMo/model/elmo_31051301'

    
DATA:
    MINIMUM_SPACE_COUNT: 2
    TOKEN_MAX_LEN: 40
    CHR_MAX_LEN: 4
    BATCH_SIZE: 64
    TOKEN_MIN_FREQ: 5
    CHR_MIN_FREQ: 1
    BATCH_SIZE: 64
MODEL:
    ELMO: True
    GRU:
        HID_DIM: 1024
        N_LAYERS: 2
        EMBEDDING_DIM: 1024

TRAIN:
    LR: 0.00005
    N_EPOCHS: 100
    DEVICE: cuda
    SCHEDULER: True